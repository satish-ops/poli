pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh ' ${WORKSPACE}//LOKP//hello.sh'
            }
        }
    }
}
